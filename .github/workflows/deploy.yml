name: Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Create .env file
      env:
        ENV: ${{ secrets.ENV }}
        VPS_SSH_USER: ${{ secrets.VPS_SSH_USER }}
      run: |
        echo "ENV=${ENV}" >> .env
        echo "DEBUG=${{ secrets.DEBUG }}" >> .env
        echo "SECRET_KEY=${{ secrets.SECRET_KEY }}" >> .env
        echo "ALLOWED_HOSTS=${{ secrets.ALLOWED_HOSTS }}" >> .env
        echo "CORS_ALLOWED_ORIGINS=${{ secrets.CORS_ALLOWED_ORIGINS }}" >> .env
        echo "TIME_ZONE=${{ secrets.TIME_ZONE }}" >> .env
        echo "LANGUAGE_CODE=${{ secrets.LANGUAGE_CODE }}" >> .env
        # Database settings (uncomment if using external PostgreSQL)
        # echo "DATABASE_URL=${{ secrets.DATABASE_URL }}" >> .env

    - name: Copy files to VPS
      uses: easingthemes/ssh-deploy@main
      with:
        SSH_PRIVATE_KEY: ${{ secrets.VPS_SSH_KEY }}
        ARGS: "-rlgoDzvc -i"
        SOURCE: "./"
        REMOTE_HOST: ${{ secrets.VPS_HOST }}
        REMOTE_USER: ${{ secrets.VPS_SSH_USER }}
        TARGET: '/home/${{ secrets.VPS_SSH_USER }}/salao-backend'
        EXCLUDE: "/dist/, /node_modules/, /.git/, /venv/, /__pycache__/, *.pyc"
        SCRIPT_AFTER: |
          echo "Files copied successfully"
          echo "Cleaning up unused Docker resources..."
          docker system prune -f
          
          echo "Stopping existing containers..."
          cd /home/${{ secrets.VPS_SSH_USER }}/salao-backend
          docker compose down || true

    - name: Build and start containers
      uses: appleboy/ssh-action@v0.1.10
      with:
        host: ${{ secrets.VPS_HOST }}
        username: ${{ secrets.VPS_SSH_USER }}
        key: ${{ secrets.VPS_SSH_KEY }}
        script: |
          cd /home/${{ secrets.VPS_SSH_USER }}/salao-backend
          
          echo "Building and starting containers..."
          docker compose up -d --build
          
          echo "Waiting for services to start..."
          sleep 30
          
          echo "Checking container status..."
          docker compose ps
          
          echo "Deployment completed successfully!"

    - name: Health check
      uses: appleboy/ssh-action@v0.1.10
      with:
        host: ${{ secrets.VPS_HOST }}
        username: ${{ secrets.VPS_SSH_USER }}
        key: ${{ secrets.VPS_SSH_KEY }}
        script: |
          echo "Performing health check..."
          sleep 10
          
          # Check if nginx is responding locally on VPS
          echo "Testing application health..."
          
          # Test nginx is serving requests
          if curl -f -s http://localhost/ > /dev/null 2>&1; then
            echo "✅ Nginx is responding"
          else
            echo "❌ Nginx is not responding"
            docker compose logs salao-nginx --tail=20
          fi
          # Test Django admin is accessible
          if curl -f -s http://localhost/admin/ > /dev/null 2>&1; then
            echo "✅ Django admin is accessible"
          else
            echo "⚠️  Django admin check failed (might be normal if no admin configured)"
            docker compose logs salao-backend --tail=20
          fi
          
          # Test API endpoint
          if curl -f -s http://localhost/api/ > /dev/null 2>&1; then
            echo "✅ API endpoint is responding"
          else
            echo "⚠️  API endpoint check failed"
            docker compose logs salao-backend --tail=20
          fi
          
          # Show container status
          echo "\nContainer status:"
          docker compose ps
          
          # Show brief logs
          echo "\nRecent logs:"
          docker compose logs --tail=10
          
          echo "\n🎉 Deployment health check completed!"
          echo "Your application should be accessible at: app2.andrepombo.info"
